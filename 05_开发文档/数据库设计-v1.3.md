# 数据库设计（朋友验证版）

**版本**：v1.3  
**创建日期**：2026-02-05  
**最后更新**：2026-02-05  
**本次更新**：基于朋友验证路径简化设计，每个朋友独立数据库  
**维护人**：技术团队

---

## 1. 设计原则

### 1.1 简化设计（朋友验证版）

**核心原则**：
- 每个朋友独立数据库文件（SQLite）
- 简化表结构，聚焦核心功能
- 去掉内测阶段不需要的复杂功能
- 便于独立部署和数据隔离

### 1.2 表结构简化

**去掉的表**：
- ❌ 复杂的审计日志表
- ❌ 分佣相关表（订单、对账）
- ❌ 多租户复杂权限表
- ❌ 复杂的版本管理表

**保留的表**：
- ✅ 租户表（简化版）
- ✅ 客户表（13个必填字段）
- ✅ 诊断记录表（六宫格评分）
- ✅ 改造方案表（方案记录）
- ✅ 操作日志表（简化版）
- ✅ 使用统计表（朋友使用数据）

### 1.3 独立部署优势

**数据隔离**：
- 每个朋友独立数据库文件
- 物理隔离，更安全
- 朋友更信任

**部署简单**：
- 无需复杂的多租户逻辑
- 每个实例独立运行
- 一键部署脚本

**维护方便**：
- 一个朋友出问题不影响其他
- 独立备份恢复
- 独立监控日志

---

## 2. 核心表设计

### 2.1 租户表（tenants）

```sql
-- 每个朋友一个租户（独立部署时每个实例只有一条记录）
CREATE TABLE tenants (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    
    -- 基础信息
    name TEXT NOT NULL UNIQUE,           -- 朋友姓名/公司名称
    logo_path TEXT,                      -- Logo文件路径
    primary_color TEXT DEFAULT '#1890ff', -- 主色调
    secondary_color TEXT DEFAULT '#52c41a', -- 辅色调
    
    -- 配置信息
    config_json TEXT DEFAULT '{}',       -- JSON格式配置
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 简化：每个实例只有一个租户，所以不需要复杂的租户管理
```

### 2.2 客户表（customers）

```sql
-- 客户基本信息（13个必填字段）
CREATE TABLE customers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id INTEGER NOT NULL,
    
    -- 13个必填字段
    name TEXT NOT NULL,                  -- 企业名称
    reg_capital REAL,                    -- 注册资本（万元）
    established_date DATE,               -- 成立时间
    legal_person TEXT,                   -- 法人姓名
    
    annual_invoice REAL,                 -- 年开票（万元）
    annual_tax REAL,                     -- 年纳税（万元）
    monthly_flow REAL,                   -- 对公流水月均（万元）
    
    legal_credit TEXT,                   -- 法人征信（无逾期/M1逾期/M3+逾期）
    company_credit TEXT,                 -- 企业征信（正常/异常）
    
    main_business TEXT,                  -- 主营业务
    industry TEXT,                       -- 行业
    
    loan_demand REAL,                    -- 贷款需求（万元）
    collateral TEXT,                     -- 抵押物情况（有房产/150万/无）
    
    -- 状态信息
    status TEXT DEFAULT 'active',        -- active/archived/deleted
    category TEXT,                       -- A/B/C（诊断结果）
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (tenant_id) REFERENCES tenants(id)
);

-- 索引
CREATE INDEX idx_customers_tenant_id ON customers(tenant_id);
CREATE INDEX idx_customers_category ON customers(category);
CREATE INDEX idx_customers_created_at ON customers(created_at);
```

### 2.3 诊断记录表（diagnosis_records）

```sql
-- 诊断记录（每次诊断生成一条记录）
CREATE TABLE diagnosis_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id INTEGER NOT NULL,
    customer_id INTEGER NOT NULL,
    
    -- 六宫格得分
    flow_score INTEGER DEFAULT 0,        -- 流水体质得分（0-200）
    invoice_tax_score INTEGER DEFAULT 0, -- 票税体质得分（0-200）
    credit_score INTEGER DEFAULT 0,      -- 征信体质得分（0-200）
    asset_score INTEGER DEFAULT 0,       -- 资产体质得分（0-150）
    qualification_score INTEGER DEFAULT 0, -- 资质体质得分（0-150）
    industry_score INTEGER DEFAULT 0,    -- 行业体质得分（0-100）
    
    total_score INTEGER DEFAULT 0,       -- 总分（0-1000）
    category TEXT CHECK(category IN ('A', 'B', 'C')), -- A/B/C分类
    
    -- 诊断详情
    diagnosis_reason TEXT,               -- 诊断理由（JSON格式）
    raw_data_json TEXT,                  -- 原始数据（JSON格式，用于追溯）
    
    -- 人工复核
    manual_reviewed BOOLEAN DEFAULT FALSE, -- 是否人工复核
    manual_category TEXT,                -- 人工分类（A/B/C）
    manual_reason TEXT,                  -- 人工复核理由
    reviewed_by TEXT,                    -- 复核人
    reviewed_at TIMESTAMP,               -- 复核时间
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (tenant_id) REFERENCES tenants(id),
    FOREIGN KEY (customer_id) REFERENCES customers(id)
);

-- 索引
CREATE INDEX idx_diagnosis_tenant_id ON diagnosis_records(tenant_id);
CREATE INDEX idx_diagnosis_customer_id ON diagnosis_records(customer_id);
CREATE INDEX idx_diagnosis_category ON diagnosis_records(category);
CREATE INDEX idx_diagnosis_created_at ON diagnosis_records(created_at);
```

### 2.4 改造方案表（improvement_plans）

```sql
-- 改造方案记录
CREATE TABLE improvement_plans (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id INTEGER NOT NULL,
    customer_id INTEGER NOT NULL,
    diagnosis_id INTEGER NOT NULL,
    
    -- 方案信息
    plan_type TEXT NOT NULL,             -- 方案类型（开票提升/纳税提升等）
    plan_name TEXT,                      -- 方案名称
    plan_description TEXT,               -- 方案描述
    
    -- 方案内容（JSON格式）
    gap_list_json TEXT,                  -- 缺口清单
    task_list_json TEXT,                 -- 任务清单
    service_providers_json TEXT,         -- 推荐服务商
    effect_preview_json TEXT,            -- 效果预览
    cost_estimation_json TEXT,           -- 成本估算
    
    -- 状态跟踪
    status TEXT DEFAULT 'pending',       -- pending/in_progress/completed/cancelled
    progress_percentage INTEGER DEFAULT 0, -- 进度百分比（0-100）
    
    -- 执行信息
    started_at TIMESTAMP,                -- 开始时间
    completed_at TIMESTAMP,              -- 完成时间
    cancelled_at TIMESTAMP,              -- 取消时间
    cancelled_reason TEXT,               -- 取消原因
    
    -- 提醒设置
    reminder_enabled BOOLEAN DEFAULT TRUE, -- 是否启用提醒
    reminder_days INTEGER DEFAULT 90,    -- 提醒天数（默认90天）
    last_reminded_at TIMESTAMP,          -- 上次提醒时间
    next_remind_at TIMESTAMP,            -- 下次提醒时间
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (tenant_id) REFERENCES tenants(id),
    FOREIGN KEY (customer_id) REFERENCES customers(id),
    FOREIGN KEY (diagnosis_id) REFERENCES diagnosis_records(id)
);

-- 索引
CREATE INDEX idx_plans_tenant_id ON improvement_plans(tenant_id);
CREATE INDEX idx_plans_customer_id ON improvement_plans(customer_id);
CREATE INDEX idx_plans_diagnosis_id ON improvement_plans(diagnosis_id);
CREATE INDEX idx_plans_status ON improvement_plans(status);
CREATE INDEX idx_plans_next_remind ON improvement_plans(next_remind_at);
```

### 2.5 操作日志表（operation_logs）

```sql
-- 简化版操作日志（基础审计）
CREATE TABLE operation_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id INTEGER NOT NULL,
    
    -- 操作信息
    operation_type TEXT NOT NULL,         -- create/update/delete/import/diagnosis/plan
    resource_type TEXT NOT NULL,         -- customer/diagnosis/plan
    resource_id INTEGER,                 -- 资源ID
    
    -- 操作详情
    operator TEXT DEFAULT 'system',      -- 操作人（默认system）
    details_json TEXT,                   -- 操作详情（JSON格式）
    ip_address TEXT,                     -- IP地址（可选）
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (tenant_id) REFERENCES tenants(id)
);

-- 索引
CREATE INDEX idx_logs_tenant_id ON operation_logs(tenant_id);
CREATE INDEX idx_logs_operation_type ON operation_logs(operation_type);
CREATE INDEX idx_logs_resource_type ON operation_logs(resource_type);
CREATE INDEX idx_logs_created_at ON operation_logs(created_at);
```

### 2.6 使用统计表（usage_stats）

```sql
-- 朋友使用数据统计（用于验证）
CREATE TABLE usage_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id INTEGER NOT NULL,
    
    -- 统计维度
    stat_date DATE NOT NULL,             -- 统计日期
    stat_type TEXT NOT NULL,             -- daily/weekly/monthly
    
    -- 使用数据
    login_count INTEGER DEFAULT 0,       -- 登录次数
    active_days INTEGER DEFAULT 0,       -- 活跃天数
    
    -- 功能使用
    import_count INTEGER DEFAULT 0,     -- 导入次数
    diagnosis_count INTEGER DEFAULT 0,   -- 诊断次数
    plan_generation_count INTEGER DEFAULT 0, -- 方案生成次数
    export_count INTEGER DEFAULT 0,      -- 导出次数
    
    -- 客户数据
    customer_count INTEGER DEFAULT 0,    -- 客户总数
    customer_a_count INTEGER DEFAULT 0,  -- A类客户数
    customer_b_count INTEGER DEFAULT 0,  -- B类客户数
    customer_c_count INTEGER DEFAULT 0,  -- C类客户数
    
    -- 时间戳
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE(tenant_id, stat_date, stat_type),
    FOREIGN KEY (tenant_id) REFERENCES tenants(id)
);

-- 索引
CREATE INDEX idx_stats_tenant_id ON usage_stats(tenant_id);
CREATE INDEX idx_stats_stat_date ON usage_stats(stat_date);
CREATE INDEX idx_stats_stat_type ON usage_stats(stat_type);
```

---

## 3. 数据库初始化脚本

### 3.1 初始化脚本（init_db.py）

```python
#!/usr/bin/env python3
"""
数据库初始化脚本
每个朋友实例独立运行，创建自己的数据库
"""

import sqlite3
import os
from pathlib import Path
import json
from datetime import datetime

def init_database(db_path: str, tenant_name: str):
    """初始化朋友数据库"""
    
    # 确保目录存在
    os.makedirs(os.path.dirname(db_path), exist_ok=True)
    
    # 连接数据库
    conn = sqlite3.connect(db_path)
    cursor = conn.cursor()
    
    print(f"初始化数据库: {db_path}")
    
    # 启用外键约束
    cursor.execute("PRAGMA foreign_keys = ON")
    
    # 创建表
    print("创建表结构...")
    
    # 租户表（每个实例只有一条记录）
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS tenants (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL UNIQUE,
        logo_path TEXT,
        primary_color TEXT DEFAULT '#1890ff',
        secondary_color TEXT DEFAULT '#52c41a',
        config_json TEXT DEFAULT '{}',
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    )
    ''')
    
    # 客户表
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS customers (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        tenant_id INTEGER NOT NULL,
        name TEXT NOT NULL,
        reg_capital REAL,
        established_date DATE,
        legal_person TEXT,
        annual_invoice REAL,
        annual_tax REAL,
        monthly_flow REAL,
        legal_credit TEXT,
        company_credit TEXT,
        main_business TEXT,
        industry TEXT,
        loan_demand REAL,
        collateral TEXT,
        status TEXT DEFAULT 'active',
        category TEXT,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (tenant_id) REFERENCES tenants(id)
    )
    ''')
    
    # 诊断记录表
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS diagnosis_records (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        tenant_id INTEGER NOT NULL,
        customer_id INTEGER NOT NULL,
        flow_score INTEGER DEFAULT 0,
        invoice_tax_score INTEGER DEFAULT 0,
        credit_score INTEGER DEFAULT 0,
        asset_score INTEGER DEFAULT 0,
        qualification_score INTEGER DEFAULT 0,
        industry_score INTEGER DEFAULT 0,
        total_score INTEGER DEFAULT 0,
        category TEXT CHECK(category IN ('A', 'B', 'C')),
        diagnosis_reason TEXT,
        raw_data_json TEXT,
        manual_reviewed BOOLEAN DEFAULT FALSE,
        manual_category TEXT,
        manual_reason TEXT,
        reviewed_by TEXT,
        reviewed_at TIMESTAMP,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (tenant_id) REFERENCES tenants(id),
        FOREIGN KEY (customer_id) REFERENCES customers(id)
    )
    ''')
    
    # 改造方案表
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS improvement_plans (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        tenant_id INTEGER NOT NULL,
        customer_id INTEGER NOT NULL,
        diagnosis_id INTEGER NOT NULL,
        plan_type TEXT NOT NULL,
        plan_name TEXT,
        plan_description TEXT,
        gap_list_json TEXT,
        task_list_json TEXT,
        service_providers_json TEXT,
        effect_preview_json TEXT,
        cost_estimation_json TEXT,
        status TEXT DEFAULT 'pending',
        progress_percentage INTEGER DEFAULT 0,
        started_at TIMESTAMP,
        completed_at TIMESTAMP,
        cancelled_at TIMESTAMP,
        cancelled_reason TEXT,
        reminder_enabled BOOLEAN DEFAULT TRUE,
        reminder_days INTEGER DEFAULT 90,
        last_reminded_at TIMESTAMP,
        next_remind_at TIMESTAMP,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (tenant_id) REFERENCES tenants(id),
        FOREIGN KEY (customer_id) REFERENCES customers(id),
        FOREIGN KEY (diagnosis_id) REFERENCES diagnosis_records(id)
    )
    ''')
    
    # 操作日志表
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS operation_logs (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        tenant_id INTEGER NOT NULL,
        operation_type TEXT NOT NULL,
        resource_type TEXT NOT NULL,
        resource_id INTEGER,
        operator TEXT DEFAULT 'system',
        details_json TEXT,
        ip_address TEXT,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (tenant_id) REFERENCES tenants(id)
    )
    ''')
    
    # 使用统计表
    cursor.execute('''
    CREATE TABLE IF NOT EXISTS usage_stats (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        tenant_id INTEGER NOT NULL,
        stat_date DATE NOT NULL,
        stat_type TEXT NOT NULL,
        login_count INTEGER DEFAULT 0,
        active_days INTEGER DEFAULT 0,
        import_count INTEGER DEFAULT 0,
        diagnosis_count INTEGER DEFAULT 0,
        plan_generation_count INTEGER DEFAULT 0,
        export_count INTEGER DEFAULT 0,
        customer_count INTEGER DEFAULT 0,
        customer_a_count INTEGER DEFAULT 0,
        customer_b_count INTEGER DEFAULT 0,
        customer_c_count INTEGER DEFAULT 0,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(tenant_id, stat_date, stat_type),
        FOREIGN KEY (tenant_id) REFERENCES tenants(id)
    )
    ''')
    
    # 创建索引
    print("创建索引...")
    
    # 客户表索引
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_customers_tenant_id ON customers(tenant_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_customers_category ON customers(category)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_customers_created_at ON customers(created_at)')
    
    # 诊断记录表索引
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_diagnosis_tenant_id ON diagnosis_records(tenant_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_diagnosis_customer_id ON diagnosis_records(customer_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_diagnosis_category ON diagnosis_records(category)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_diagnosis_created_at ON diagnosis_records(created_at)')
    
    # 改造方案表索引
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_plans_tenant_id ON improvement_plans(tenant_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_plans_customer_id ON improvement_plans(customer_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_plans_diagnosis_id ON improvement_plans(diagnosis_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_plans_status ON improvement_plans(status)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_plans_next_remind ON improvement_plans(next_remind_at)')
    
    # 操作日志表索引
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_logs_tenant_id ON operation_logs(tenant_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_logs_operation_type ON operation_logs(operation_type)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_logs_resource_type ON operation_logs(resource_type)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_logs_created_at ON operation_logs(created_at)')
    
    # 使用统计表索引
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_stats_tenant_id ON usage_stats(tenant_id)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_stats_stat_date ON usage_stats(stat_date)')
    cursor.execute('CREATE INDEX IF NOT EXISTS idx_stats_stat_type ON usage_stats(stat_type)')
    
    # 插入默认租户记录
    print("插入默认租户记录...")
    cursor.execute('''
    INSERT OR IGNORE INTO tenants (name, logo_path, primary_color, secondary_color, config_json)
    VALUES (?, ?, ?, ?, ?)
    ''', (
        tenant_name,
        '/app/data/logo.png',
        '#1890ff',
        '#52c41a',
        json.dumps({
            'reminder_enabled': True,
            'export_format': 'excel',
            'diagnosis_auto': True
        })
    ))
    
    # 创建默认管理员用户（简化版，V0只有单一管理员）
    print("创建默认管理员用户...")
    # 在实际代码中，这里会创建用户表并插入默认用户
    # 但V0简化版中，我们使用硬编码的单一管理员
    
    # 提交更改
    conn.commit()
    
    # 验证表创建
    cursor.execute("SELECT name FROM sqlite_master WHERE type='table'")
    tables = cursor.fetchall()
    print(f"创建的表: {[table[0] for table in tables]}")
    
    # 关闭连接
    conn.close()
    
    print(f"✅ 数据库初始化完成: {db_path}")
    print(f"默认租户: {tenant_name}")
    print(f"数据库文件: {db_path}")

if __name__ == "__main__":
    # 使用示例
    db_path = "data/database.db"
    tenant_name = "默认租户"
    
    init_database(db_path, tenant_name)
```

### 3.2 数据库迁移脚本（migration.py）

```python
#!/usr/bin/env python3
"""
数据库迁移脚本
用于后续版本升级时的数据迁移
"""

import sqlite3
import os
from pathlib import Path

def migrate_v1_to_v2(db_path: str):
    """从V1迁移到V2（如果需要）"""
    conn = sqlite3.connect(db_path)
    cursor = conn.cursor()
    
    print(f"开始数据库迁移: {db_path}")
    
    try:
        # 检查是否需要迁移
        cursor.execute("PRAGMA user_version")
        current_version = cursor.fetchone()[0]
        
        if current_version < 2:
            print(f"当前版本: {current_version}, 升级到版本2...")
            
            # 添加新字段或修改表结构
            # 例如：添加新字段到customers表
            try:
                cursor.execute("ALTER TABLE customers ADD COLUMN test_field TEXT")
                print("添加test_field字段到customers表")
            except sqlite3.OperationalError as e:
                if "duplicate column name" not in str(e):
                    raise
            
            # 更新版本号
            cursor.execute("PRAGMA user_version = 2")
            conn.commit()
            print("✅ 迁移完成，版本升级到2")
        else:
            print(f"当前版本已经是{current_version}，无需迁移")
            
    except Exception as e:
        print(f"迁移失败: {e}")
        conn.rollback()
        raise
    finally:
        conn.close()

if __name__ == "__main__":
    # 迁移所有朋友数据库
    friends_dir = Path("friends")
    
    if friends_dir.exists():
        for friend_dir in friends_dir.iterdir():
            if friend_dir.is_dir():
                db_path = friend_dir / "data" / "database.db"
                if db_path.exists():
                    print(f"\n迁移朋友数据库: {friend_dir.name}")
                    migrate_v1_to_v2(str(db_path))
    else:
        print("未找到friends目录，跳过迁移")
```

---

## 4. 数据备份与恢复

### 4.1 备份脚本（backup.sh）

```bash
#!/bin/bash
# 数据库备份脚本
# 每天凌晨3点自动运行

set -e

FRIEND_NAME="$1"
BACKUP_DIR="backups/$FRIEND_NAME"
DATE=$(date '+%Y%m%d_%H%M%S')
DB_PATH="friends/friend_$FRIEND_NAME/data/database.db"

echo "📦 开始备份 $FRIEND_NAME 的数据库..."

# 创建备份目录
mkdir -p "$BACKUP_DIR"

# 备份数据库
if [ -f "$DB_PATH" ]; then
    cp "$DB_PATH" "$BACKUP_DIR/database_$DATE.db"
    echo "✅ 数据库备份完成: $BACKUP_DIR/database_$DATE.db"
else
    echo "⚠️ 数据库文件不存在: $DB_PATH"
fi

# 备份上传文件（如果有）
UPLOADS_PATH="friends/friend_$FRIEND_NAME/data/uploads"
if [ -d "$UPLOADS_PATH" ]; then
    tar -czf "$BACKUP_DIR/uploads_$DATE.tar.gz" -C "$UPLOADS_PATH" .
    echo "✅ 上传文件备份完成: $BACKUP_DIR/uploads_$DATE.tar.gz"
fi

# 清理旧备份（保留最近7天）
find "$BACKUP_DIR" -name "*.db" -mtime +7 -delete
find "$BACKUP_DIR" -name "*.tar.gz" -mtime +7 -delete

echo "✅ $FRIEND_NAME 备份完成！"
```

### 4.2 恢复脚本（restore.sh）

```bash
#!/bin/bash
# 数据库恢复脚本

set -e

FRIEND_NAME="$1"
BACKUP_FILE="$2"
DB_PATH="friends/friend_$FRIEND_NAME/data/database.db"

echo "🔄 开始恢复 $FRIEND_NAME 的数据库..."

if [ ! -f "$BACKUP_FILE" ]; then
    echo "❌ 备份文件不存在: $BACKUP_FILE"
    exit 1
fi

# 停止服务（避免数据不一致）
cd "friends/friend_$FRIEND_NAME"
docker-compose stop backend

# 恢复数据库
cp "$BACKUP_FILE" "$DB_PATH"
echo "✅ 数据库恢复完成: $DB_PATH"

# 启动服务
docker-compose start backend

echo "✅ $FRIEND_NAME 恢复完成！服务已重启。"
```

---

## 5. 性能优化

### 5.1 索引设计

**核心查询索引**：
```sql
-- 客户列表查询（按时间倒序）
CREATE INDEX idx_customers_tenant_created ON customers(tenant_id, created_at DESC);

-- 诊断记录查询（按客户和时间）
CREATE INDEX idx_diagnosis_customer_created ON diagnosis_records(customer_id, created_at DESC);

-- 方案查询（按状态和提醒时间）
CREATE INDEX idx_plans_status_remind ON improvement_plans(status, next_remind_at);

-- 使用统计查询（按租户和日期）
CREATE INDEX idx_stats_tenant_date ON usage_stats(tenant_id, stat_date DESC);
```

### 5.2 查询优化

**分页查询优化**：
```sql
-- 客户列表分页查询
SELECT * FROM customers 
WHERE tenant_id = ? 
ORDER BY created_at DESC 
LIMIT ? OFFSET ?;

-- 使用覆盖索引
CREATE INDEX idx_customers_tenant_created_paginate 
ON customers(tenant_id, created_at DESC, id);
```

**统计查询优化**：
```sql
-- 每日使用统计（预计算）
INSERT INTO usage_stats (tenant_id, stat_date, stat_type, ...)
SELECT 
    tenant_id,
    DATE(created_at) as stat_date,
    'daily' as stat_type,
    COUNT(DISTINCT DATE(created_at)) as active_days,
    COUNT(*) as diagnosis_count
FROM diagnosis_records
WHERE tenant_id = ? AND DATE(created_at) = ?
GROUP BY tenant_id, DATE(created_at)
ON CONFLICT(tenant_id, stat_date, stat_type) 
DO UPDATE SET 
    diagnosis_count = excluded.diagnosis_count,
    updated_at = CURRENT_TIMESTAMP;
```

### 5.3 数据库维护

**定期清理**：
```sql
-- 清理30天前的操作日志
DELETE FROM operation_logs 
WHERE created_at < DATE('now', '-30 days');

-- 清理软删除的客户（保留90天）
UPDATE customers 
SET status = 'deleted_permanent'
WHERE status = 'deleted' 
AND updated_at < DATE('now', '-90 days');

DELETE FROM customers 
WHERE status = 'deleted_permanent';
```

**数据库优化**：
```sql
-- 定期执行VACUUM（每月一次）
VACUUM;

-- 重新分析统计信息（每周一次）
ANALYZE;
```

---

## 6. 安全考虑

### 6.1 数据安全

**文件权限**：
```bash
# 数据库文件权限设置
chmod 600 data/database.db          # 只有所有者可读写
chmod 700 data/                     # 只有所有者可访问
chmod 755 backups/                  # 所有者可读写，其他只读
```

**备份加密**：
```bash
# 备份时加密（可选）
openssl enc -aes-256-cbc -salt -in database.db -out database.db.enc -pass pass:your_password
```

### 6.2 访问控制

**SQLite配置**：
```python
# 连接数据库时设置配置
import sqlite3

conn = sqlite3.connect('data/database.db')
conn.execute("PRAGMA journal_mode = WAL")  # 写前日志
conn.execute("PRAGMA synchronous = NORMAL") # 同步模式
conn.execute("PRAGMA foreign_keys = ON")   # 外键约束
conn.execute("PRAGMA busy_timeout = 5000") # 超时设置
```

### 6.3 监控告警

**磁盘空间监控**：
```python
import os
import sqlite3

def check_disk_space(db_path):
    """检查数据库磁盘空间"""
    stat = os.statvfs(os.path.dirname(db_path))
    free_gb = stat.f_bavail * stat.f_frsize / 1024**3
    
    if free_gb < 1:  # 小于1GB
        send_alert(f"磁盘空间不足: {free_gb:.2f}GB")
    
    # 检查数据库大小
    db_size_mb = os.path.getsize(db_path) / 1024**2
    if db_size_mb > 100:  # 大于100MB
        send_alert(f"数据库过大: {db_size_mb:.2f}MB")
```

---

## 7. 部署配置

### 7.1 数据库配置（config.py）

```python
# backend/app/config.py
import os
from pathlib import Path

class Config:
    # 数据库配置
    DATABASE_PATH = os.getenv('DB_PATH', 'data/database.db')
    
    # 确保数据库目录存在
    db_dir = Path(DATABASE_PATH).parent
    db_dir.mkdir(parents=True, exist_ok=True)
    
    # SQLite连接字符串
    SQLALCHEMY_DATABASE_URL = f"sqlite:///{DATABASE_PATH}"
    
    # 连接池配置
    SQLALCHEMY_ENGINE_OPTIONS = {
        "connect_args": {"check_same_thread": False},
        "pool_pre_ping": True,
        "pool_recycle": 3600,
    }
    
    # 其他配置
    UPLOAD_FOLDER = os.getenv('UPLOAD_PATH', 'data/uploads')
    MAX_CONTENT_LENGTH = 16 * 1024 * 1024  # 16MB
    SECRET_KEY = os.getenv('SECRET_KEY', 'your-secret-key-here')
    
    # 租户配置
    TENANT_NAME = os.getenv('APP_NAME', '默认租户')
    BRAND_NAME = os.getenv('BRAND_NAME', '贷款助手')
    LOGO_PATH = os.getenv('LOGO_PATH', '/app/data/logo.png')
    
    # 创建上传目录
    upload_dir = Path(UPLOAD_FOLDER)
    upload_dir.mkdir(parents=True, exist_ok=True)

config = Config()
```

### 7.2 数据库连接（database.py）

```python
# backend/app/database.py
from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker

from app.config import config

# 创建数据库引擎
engine = create_engine(
    config.SQLALCHEMY_DATABASE_URL,
    connect_args={"check_same_thread": False},
    pool_pre_ping=True,
    pool_recycle=3600,
)

# 创建会话工厂
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

# 声明基类
Base = declarative_base()

def get_db():
    """获取数据库会话"""
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

def init_db():
    """初始化数据库（创建表）"""
    Base.metadata.create_all(bind=engine)
    
def check_db_connection():
    """检查数据库连接"""
    try:
        with engine.connect() as conn:
            conn.execute("SELECT 1")
        return True
    except Exception as e:
        print(f"数据库连接失败: {e}")
        return False
```

---

## 8. 使用示例

### 8.1 创建客户记录

```python
# backend/app/services/customer_service.py
from sqlalchemy.orm import Session
from datetime import date
from app.models.customer import Customer

def create_customer(db: Session, tenant_id: int, customer_data: dict):
    """创建客户记录"""
    
    customer = Customer(
        tenant_id=tenant_id,
        name=customer_data['name'],
        reg_capital=customer_data.get('reg_capital'),
        established_date=customer_data.get('established_date'),
        legal_person=customer_data.get('legal_person'),
        annual_invoice=customer_data.get('annual_invoice'),
        annual_tax=customer_data.get('annual_tax'),
        monthly_flow=customer_data.get('monthly_flow'),
        legal_credit=customer_data.get('legal_credit'),
        company_credit=customer_data.get('company_credit'),
        main_business=customer_data.get('main_business'),
        industry=customer_data.get('industry'),
        loan_demand=customer_data.get('loan_demand'),
        collateral=customer_data.get('collateral'),
        status='active'
    )
    
    db.add(customer)
    db.commit()
    db.refresh(customer)
    
    # 记录操作日志
    log_operation(
        db=db,
        tenant_id=tenant_id,
        operation_type='create',
        resource_type='customer',
        resource_id=customer.id,
        details=customer_data
    )
    
    return customer
```

### 8.2 执行诊断

```python
# backend/app/services/diagnosis_service.py
from sqlalchemy.orm import Session
from app.models.customer import Customer
from app.models.diagnosis_record import DiagnosisRecord

def diagnose_customer(db: Session, customer_id: int):
    """诊断客户"""
    
    # 获取客户信息
    customer = db.query(Customer).filter(Customer.id == customer_id).first()
    if not customer:
        return None
    
    # 计算六宫格得分
    scores = calculate_scores(customer)
    
    # 计算总分
    total_score = sum([
        scores['flow_score'],
        scores['invoice_tax_score'],
        scores['credit_score'],
        scores['asset_score'],
        scores['qualification_score'],
        scores['industry_score']
    ])
    
    # 确定分类
    if total_score >= 600:
        category = 'A'
    elif total_score >= 400:
        category = 'B'
    else:
        category = 'C'
    
    # 一票否决项检查
    if customer.legal_credit == 'M3+逾期':
        category = 'C'
    
    # 创建诊断记录
    diagnosis = DiagnosisRecord(
        tenant_id=customer.tenant_id,
        customer_id=customer_id,
        flow_score=scores['flow_score'],
        invoice_tax_score=scores['invoice_tax_score'],
        credit_score=scores['credit_score'],
        asset_score=scores['asset_score'],
        qualification_score=scores['qualification_score'],
        industry_score=scores['industry_score'],
        total_score=total_score,
        category=category,
        diagnosis_reason=generate_reason(scores, category),
        raw_data_json=customer.to_dict()
    )
    
    db.add(diagnosis)
    db.commit()
    db.refresh(diagnosis)
    
    # 更新客户分类
    customer.category = category
    db.commit()
    
    # 记录操作日志
    log_operation(
        db=db,
        tenant_id=customer.tenant_id,
        operation_type='diagnosis',
        resource_type='customer',
        resource_id=customer_id,
        details={
            'scores': scores,
            'total_score': total_score,
            'category': category
        }
    )
    
    return diagnosis
```

---

## 9. 常见问题

### Q1：SQLite能支撑多少数据？

**A1**：
- **单个朋友**：支持1000-5000个客户记录
- **诊断记录**：每个客户多次诊断，支持10000+记录
- **方案记录**：每个客户多个方案，支持5000+记录
- **性能表现**：100条数据导入<5秒，诊断计算<2秒

### Q2：如何备份和恢复？

**A2**：
- **自动备份**：每天凌晨3点自动备份
- **手动备份**：运行`./scripts/backup.sh [朋友姓名]`
- **恢复数据**：运行`./scripts/restore.sh [朋友姓名] [备份文件]`
- **保留策略**：保留最近7天备份

### Q3：如何迁移到PostgreSQL？

**A3**：
如果朋友数量增加或数据量增大，可以迁移到PostgreSQL：

```python
# migration_sqlite_to_postgres.py
import sqlite3
import psycopg2
import json

def migrate_to_postgres(sqlite_path, postgres_url):
    """从SQLite迁移到PostgreSQL"""
    
    # 连接SQLite
    sqlite_conn = sqlite3.connect(sqlite_path)
    sqlite_cursor = sqlite_conn.cursor()
    
    # 连接PostgreSQL
    postgres_conn = psycopg2.connect(postgres_url)
    postgres_cursor = postgres_conn.cursor()
    
    # 迁移数据
    tables = ['tenants', 'customers', 'diagnosis_records', 'improvement_plans', 'operation_logs', 'usage_stats']
    
    for table in tables:
        print(f"迁移表: {table}")
        
        # 从SQLite读取数据
        sqlite_cursor.execute(f"SELECT * FROM {table}")
        rows = sqlite_cursor.fetchall()
        
        # 获取列名
        sqlite_cursor.execute(f"PRAGMA table_info({table})")
        columns = [col[1] for col in sqlite_cursor.fetchall()]
        
        # 插入到PostgreSQL
        for row in rows:
            placeholders = ', '.join(['%s'] * len(row))
            column_names = ', '.join(columns)
            query = f"INSERT INTO {table} ({column_names}) VALUES ({placeholders})"
            postgres_cursor.execute(query, row)
    
    # 提交和关闭
    postgres_conn.commit()
    postgres_cursor.close()
    postgres_conn.close()
    sqlite_cursor.close()
    sqlite_conn.close()
    
    print("✅ 迁移完成")
```

---

**文档版本**：v1.3  
**创建日期**：2026-02-05  
**维护人**：技术团队  
**适用阶段**：Phase 0（朋友验证阶段）  
**数据库引擎**：SQLite（每个朋友独立文件）  
**后续升级**：V1阶段考虑迁移到PostgreSQL